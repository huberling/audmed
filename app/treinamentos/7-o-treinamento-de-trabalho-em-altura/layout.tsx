import '../../components/treinamentos-esp.css'
export default function Treinamento7Layout({children,} : {children: React.ReactNode}) {
    return (
        <div>
            {children}
        </div>
    )
  }