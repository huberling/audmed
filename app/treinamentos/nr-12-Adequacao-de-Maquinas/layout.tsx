import '../../components/treinamentos-esp.css'
export default function Treinamento5Layout({children,} : {children: React.ReactNode}) {
    return (
        <div>
            {children}
        </div>
    )
  }