import '../../components/treinamentos-esp.css'
export default function Treinamento1Layout({children,} : {children: React.ReactNode}) {
    return (
        <div>
            {children}
        </div>
    )
  }