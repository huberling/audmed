import '../../components/treinamentos-esp.css'
export default function Treinamento8Layout({children,} : {children: React.ReactNode}) {
    return (
        <div>
            {children}
        </div>
    )
  }